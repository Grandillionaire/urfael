# Urfael memory (template)

These are starter files. On install, `bootstrap-machine.sh` copies them to `~/Urfael-memory` and
inits a **private, local** git repo there. Your memory is personal — keep this repo private; never
make it public or part of the Urfael source repo.
