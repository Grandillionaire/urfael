---
description: Search the vault and answer from the user's own notes
argument-hint: <question>
---

Answer this from the user's vault:

$ARGUMENTS

1. Search the vault (filenames, frontmatter, and full text) for relevant notes. Follow
   `[[wikilinks]]` to gather connected context.
2. Synthesize a direct answer — spoken-style, headline first. Don't just list notes; tell
   the user what he knows about this.
3. Cite the note names you drew from (so he can open them).
4. If the vault has nothing on it, say so plainly and offer to research it or capture it as
   an open question — don't fabricate.
